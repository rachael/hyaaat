{
  "name": "hyaaat",
  "version": "0.1.0",
  "description": "Browser-based arena brawler where screaming = damage",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "vitest": "^1.2.0"
  }
}
